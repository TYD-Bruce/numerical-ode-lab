{
  "name": "numerical-t-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @numerical-t-lab/frontend",
    "dev:api": "tsx server/dev.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "npm run typecheck --workspace @numerical-t-lab/frontend && npm run typecheck --workspace @numerical-t-lab/numerics",
    "typecheck:api": "tsc -p tsconfig.api.json",
    "verify": "npm run test:run && npm run typecheck && npm run typecheck:api && npm run build",
    "build": "npm run typecheck && npm run build --workspace @numerical-t-lab/frontend",
    "preview": "npm run preview --workspace @numerical-t-lab/frontend"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "jsdom": "29.1.1",
    "tsx": "^4.19.0",
    "typescript": "~5.4.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}

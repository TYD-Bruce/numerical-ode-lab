# Server-side only — never commit .env or expose this in Vite (no VITE_ prefix).
OPENAI_API_KEY=your_api_key_here

# Free UI testing: mock tutor replies without calling OpenAI (no API key needed).
# AI_TUTOR_MOCK=true

# Optional: local API dev server port (default 3001)
# API_PORT=3001
